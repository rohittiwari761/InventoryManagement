# Django Configuration
DEBUG=True
SECRET_KEY=your-secret-key-here

# Database Configuration
DATABASE_NAME=inventory_db
DATABASE_USER=postgres
DATABASE_PASSWORD=your-password
DATABASE_HOST=localhost
DATABASE_PORT=5432
ALLOWED_HOSTS=localhost,127.0.0.1

# Email Configuration for Gmail
# To get an app password for Gmail:
# 1. Go to your Google Account settings
# 2. Navigate to Security
# 3. Enable 2-Step Verification if not already enabled
# 4. Generate an "App password" for Mail
# 5. Use that app password below (not your regular Gmail password)

EMAIL_HOST_PASSWORD=your-gmail-app-password-here

# Example:
# EMAIL_HOST_PASSWORD=abcd efgh ijkl mnop

# Note: The sender email is set to tiwari.rohit761@gmail.com in settings.py
# Change it in settings.py if you want to use a different sender email